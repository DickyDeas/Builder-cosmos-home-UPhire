[build]
  command = "npm --prefix uphire run build"
  publish = "uphire/dist"
  functions = "uphire/netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
